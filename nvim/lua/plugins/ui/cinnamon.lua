return {
    -- Animate Scrolling
    "declancm/cinnamon.nvim",

    enabled = false,
    version = "*", -- use latest release
    opts = {
        keymaps = {
            basic = true,
            -- extra = true,
        },
        options = { mode = "window" },
    },
}
